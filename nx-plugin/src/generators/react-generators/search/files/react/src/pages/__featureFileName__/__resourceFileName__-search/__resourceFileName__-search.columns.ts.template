import { <%= resourceClassName %>SearchRow } from './<%= resourceFileName %>-search.viewmodel';

export interface SearchColumn<T extends object> {
  field: keyof T;
  header: string;
}

export const <%= resourcePropertyName %>SearchColumns: SearchColumn<<%= resourceClassName %>SearchRow>[] = [
  {
    field: 'id',
    header: 'ID',
  },
  {
    field: 'changeMe',
    header: 'Change Me',
  },
];
