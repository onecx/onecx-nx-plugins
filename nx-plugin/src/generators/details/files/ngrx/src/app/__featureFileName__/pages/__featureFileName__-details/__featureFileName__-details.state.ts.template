import { <%= resource %> } from '../../../shared/generated';

export interface <%= featureClassName %>DetailsState {
    details: <%= resource %> | undefined;
    detailsLoadingIndicator: boolean;
    detailsLoaded: boolean;
    <%_ if(editMode){ _%>
    editMode: boolean;
    isSubmitting: boolean;
    <%_ } _%>
}
